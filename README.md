# APT-Grain-Boundary-Analysis
Takes a .pos and .rrng file.  Will then combine the two and, with user input, calculate: where the interface starts and ends, Gibbsian interfacial excess values, composition of the GB region.  Will return a .pos file of the interface region.
